<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="d-flex flex-column">
      <a class="navbar-brand" href="/">MyLOC</a>
        {% if is_granted('ROLE_ADMIN') %}
          <p class="bg-danger text-light text-end px-2 fs-6 fw-bold">ADMIN</p>
        {% elseif app.user %}
          <p class="bg-primary text-light text-end px-2 fs-6 fw-bold">{{app.user.firstName}}</p>
      {% endif %}
      
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse  " id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex align-items-center">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/tools">Outils</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#"  role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Catégories
          </a>
          <ul class="dropdown-menu">
            {% for category in global_categories.getAllCategories %}
                <li><a class="dropdown-item" href="/categories/{{category.id}}">{{ category.name }}</a></li>
            {% endfor %}
          </ul>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a href="/admin" class="btn btn-danger">Admin</a>            
          </li>
        {% elseif app.user %}
        <div class="dropdown">
          <a class="dropdown-toggle nav-link" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Mon Compte
          </a>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            <li class="dropdown-item">
              <a href="/profile/tools/add" class="btn btn-primary">Ajouter un outil + </a>
            </li>
            <li class="dropdown-item">
              <a href="/profile/tools/mytools/{{app.user.id}}" class="nav-link">Gérer mes outils</a>
            </li>
            <li class="dropdown-item">
              <a href="/profile/tools/mytools/{{app.user.id}}" class="nav-link">Mes prêts</a>
            </li>
            <li class="dropdown-item">
              <a href="/profile/tools/mytools/{{app.user.id}}" class="nav-link">Mes emprunts</a>
            </li>
            <hr>
            <li class="dropdown-item btn btn-primary">
              <a href="/profile/profile" class="btn btn-primary m-2">Mon profil</a>
            </li>
            <li class="dropdown-item">
              <a href="/logout" class="btn btn-warning m-2">Se déconnecter</a>
            </li>
          </div>
        </div>
          {# <li class="nav-item dropdown">
            <li class="nav-item">
              <a href="/profile/tools/add" class="btn btn-primary">Ajouter un outil + </a>
            </li>
            <li class="nav-item">
              <a href="/profile/tools/mytools/{{app.user.id}}" class="nav-link">Gérer mes outils</a>
            </li>
            <li class="nav-item">
              <a href="/profile/tools/mytools/{{app.user.id}}" class="nav-link">Gérer mes outils</a>
            </li>
            <li class="nav-item">
              <a href="/logout" class="btn btn-warning m-2">Se déconnecter</a>
            </li>
          </li> #}
        {% else %}
          <li class="nav-item">
            <a href="/login" class="btn btn-primary m-2">Se Connecter</a>
          </li>
          <li class="nav-item">
            <a href="/register" class="btn btn-primary m-2">Créer un Compte</a>
          </li>
        {% endif %}
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
